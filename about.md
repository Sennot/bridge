# Bot CPS Glow

A small Windows-only Geode utility for Geometry Dash 2.2081.

When a supported replay/bot mod injects a gameplay input, Bot CPS Glow makes Mega Hack's visible CPS counter pulse green. It is presentation-only: it does not inject inputs, alter TPS, change replay timing, or modify bot state.
